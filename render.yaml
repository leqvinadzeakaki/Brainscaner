services:
  - type: web
    name: brainscanner
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn busines:app
    healthCheckPath: /healthz
    envVars:
      - key: GEMINI_API_KEY
        sync: false


